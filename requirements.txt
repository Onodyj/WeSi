# Core web scraping / crawling
beautifulsoup4==4.12.3
requests==2.31.0
lxml==5.1.0
urllib3==2.2.0
Flask==3.0.0
flask-cors==5.0.0
SQLAlchemy==2.0.23

# Database
sqlalchemy==2.0.23

# Security (encrypted API key storage)
cryptography==41.0.7

# Web API framework
flask==3.0.0
flask-cors==4.0.0

# Async processing (optional — thread-based fallback used when not configured)
# Install and set USE_CELERY=true + REDIS_URL to enable
celery==5.3.4
redis==5.0.1

# AI providers (all optional — rule-based fallback used when not installed/configured)
openai==1.3.7
# anthropic  # pip install anthropic
# google-generativeai  # pip install google-generativeai

# Reports (optional)
# google-api-python-client==2.108.0
# google-auth-httplib2==0.1.1
# google-auth-oauthlib==1.1.0

# JS rendering (optional)
# playwright==1.40.0
